# 프로젝트 운영 규칙

이 문서는 측정일지 웹 프로젝트에서 지속적으로 적용되는 기술·운영 정책을 정의한다.
개별 기능의 일회성 구현 방법, 과거 장애 해결 과정, 특정 CSS 수치 등은 이 문서에 누적하지 않는다.

## 1. 문서 역할

- `AGENTS.md`: AI 에이전트의 작업·안전·Git·검증 원칙
- `project_rules.md`: 프로젝트 전반에 지속 적용되는 기술·운영 정책
- `BUSINESS_LOGIC.md`: 업무 데이터, 계산, 분류, 우선순위 등 비즈니스 규칙
- `docs/history/SUCCESS_DNA.md`: 과거 장애·원인·해결·재발방지 기록

새 규칙을 추가할 때는 성격에 맞는 문서에만 기록한다.
동일 규칙을 여러 문서에 중복 작성하지 않는다.

---

## 2. 개발 및 검증

### 2.1 개발 서버

- 로컬 개발 및 브라우저 테스트는 기본적으로 `npm run dev:turbo`를 사용한다.
- 테스트 전에 동일 프로젝트의 개발 서버가 여러 포트에서 중복 실행 중인지 확인한다.
- 실제 최신 코드가 실행 중인 포트를 확인한 뒤 테스트한다.

### 2.2 변경 범위

- 문제 해결은 관련 코드와 데이터 흐름을 확인한 뒤 최소 범위로 수정한다.
- 정상 동작하는 인접 기능을 이유 없이 리팩터링하지 않는다.
- 공통 컴포넌트 변경은 이를 사용하는 다른 화면의 회귀 가능성을 확인한다.

### 2.3 타입 및 외부 응답

- 외부 API, 라이브러리, Excel 파서 등에서 반환되는 값은 `null`, `undefined`, `false`, 빈 배열 등 비정상 또는 선택적 반환을 고려한다.
- TypeScript union type을 임의 단언으로 우회하지 않고 타입 가드를 사용한다.
- 외부 응답 구조를 추측하지 말고 실제 응답 또는 공식 타입 정의를 확인한다.

---

## 3. UI/UX 공통 원칙

- 기존 프로젝트의 공통 UI 컴포넌트와 스타일 체계를 우선 사용한다.
- 동일 기능의 입력창, 버튼, 검색 필터, 테이블은 화면 간 일관성을 유지한다.
- 특정 화면 문제를 해결하기 위한 임의의 픽셀값이나 CSS 우회 코드를 전역 규칙으로 만들지 않는다.
- 긴 텍스트, 상태 아이콘, 액션 버튼이 있는 테이블은 줄바꿈·잘림·오버플로를 실제 화면에서 확인한다.
- 인터랙티브 요소가 포함된 컨테이너에서는 부모 이벤트가 자식 버튼·입력·스크롤 동작을 막지 않도록 한다.
- 사용자에게 보이는 상태와 실제 DB 상태가 다르게 표현되지 않도록 한다.

---

## 4. 데이터 원천 및 무결성

- 동일 정보를 여러 테이블에서 얻을 수 있을 때는 업무별 원천(Source of Truth)을 명확히 정한다.
- 화면 편의를 위해 만든 가공값을 원천 데이터처럼 저장하지 않는다.
- 최신 데이터 판정은 단순 날짜뿐 아니라 완료 여부, 유효 상태 등 업무 의미를 함께 고려한다.
- Excel/MES/외부 데이터와 시스템 데이터의 우선순위는 `BUSINESS_LOGIC.md`에 정의된 정책을 따른다.
- 데이터 동기화 시 원래 존재하던 상세 데이터를 단순 상태값으로 덮어쓰지 않는다.
- 값 정규화가 필요한 필드는 저장 전 표준값으로 변환한다.
- DB schema 또는 migration이 필요한 기능은 미적용 환경에서 조용히 잘못 동작하도록 임시 우회하지 않는다. 필요한 migration과 호환 범위를 명시한다.

---

## 5. API 및 서버

- 프론트엔드에서 실제 호출하는 API를 확인한 뒤 서버 로직을 수정한다.
- 인증·권한 검증은 UI 숨김만으로 처리하지 않고 서버 API에서도 수행한다.
- 관리자 또는 특정 직무 전용 기능은 매 요청 시 서버에서 최신 권한을 확인한다.
- 조회 전용 기능은 불필요한 POST/PUT/PATCH/DELETE를 포함하지 않는다.
- 캐시가 업무 상태를 지연시키는 API는 적절한 no-store/cache-control 정책을 적용한다.
- Node.js 전용 패키지를 Next.js 서버 코드에서 사용할 때는 현재 Next.js 설정과 번들링 요구사항을 확인한다.

---

## 6. 백그라운드 작업 및 자동화

### 6.1 공통

- 웹 서버와 로컬 Windows 자동화 프로그램의 실행 책임을 분리한다.
- 외부 GUI, 로컬 파일, 네트워크 드라이브가 필요한 작업을 클라우드 서버에서 억지로 수행하지 않는다.
- 자동화 프로세스는 성공·실패·재시도·최종 상태를 명확하게 기록한다.
- 동일 작업의 중복 실행을 방지한다.

### 6.2 비대화형 프로세스

- 서버나 워커에서 실행되는 프로세스는 `input()` 등 사용자 입력을 기다리지 않는다.
- 보이지 않는 UAC 창이나 대화상자 대기로 무한 정지하지 않도록 한다.
- 외부 프로세스 종료 코드를 확인하여 성공과 실패를 구분한다.

### 6.3 Realtime 및 큐

- Realtime 이벤트는 작업 시작 신호로 사용하며 실제 작업 소유권은 claim 등 원자적 절차로 확보한다.
- 이벤트 누락에 대비한 저빈도 보완 확인을 유지한다.
- 동일 작업이 여러 워커에서 동시에 처리되지 않도록 상태 전이를 보호한다.

### 6.4 MES/K2B/문서 워커

- MES, K2B, 문서 생성 워커는 각각 독립적인 작업 상태와 실패 원인을 기록한다.
- 네트워크 드라이브 또는 외부 프로그램 장애를 정상 완료로 처리하지 않는다.
- 로컬 자동화의 파일 경로와 환경 설정은 가능한 한 환경 변수 또는 설정 파일로 관리한다.
- 자동화 완료 여부를 불필요한 임시 flag 파일에 의존하지 않는다.

---

## 7. 스케줄러 및 정기 작업

- 서버 기반 정기 작업은 가능한 한 하나의 관리 지점에서 등록한다.
- 동일한 cron 작업을 여러 프로세스에서 중복 등록하지 않는다.
- 정기 작업은 재실행되어도 데이터가 중복 생성되지 않도록 idempotent하게 설계한다.
- 시간 기반 업무 규칙은 서버 timezone을 명확히 고려한다.
- 업무상 특정 실행 시각이나 알림 조건은 `BUSINESS_LOGIC.md`에 기록한다.

---

## 8. 파일 및 저장소

- `.env.local`, credential JSON, secret key, token 등 비밀 파일은 Git에 추적하지 않는다.
- 생성형 진단 결과, 일회성 감사 JSON, 임시 스크립트, 대용량 테스트 결과를 프로젝트 루트에 장기 보관하지 않는다.
- 반복적으로 사용하는 운영 스크립트만 저장소에 유지한다.
- 과거 구현 계획서는 현재 정책 문서로 사용하지 않는다.
- 대용량 생성 파일이나 Excel 원본은 코드 저장소에 둘 필요가 있는지 먼저 검토한다.

---

## 9. 네트워크 및 로컬 경로

- `Z:\` 등 네트워크 드라이브는 항상 연결되어 있다고 가정하지 않는다.
- 네트워크 경로가 필수인 작업은 연결 실패를 명확하게 오류 처리한다.
- 로컬 fallback을 사용할 경우 원본 경로 실패 사실을 로그로 남긴다.
- 개발 PC, 깡통컴, NAS 등 장치별 경로를 코드에 무분별하게 하드코딩하지 않는다.

---

## 10. 지도·좌표·경로 기능

- 사업장 좌표는 주소 원본과 함께 관리하고 좌표 공급자를 식별할 수 있도록 한다.
- 신규 사업장 등록 시 좌표 생성 또는 좌표 보완 절차가 누락되지 않도록 한다.
- 동일 주소에 여러 사업장이 존재할 수 있으므로 좌표가 같다는 이유로 하나의 사업장으로 병합하지 않는다.
- 지도상 동일 위치 사업장은 사용자가 각각 식별하고 선택·제외할 수 있어야 한다.
- 경로 최적화는 선택된 사업장 집합과 고정 출발 조건을 보존해야 한다.
- 화면의 마커 순서와 실제 계산된 방문 순서가 일치해야 한다.
- 좌표 변환 실패·의심 좌표는 임의의 정상 좌표로 대체하지 않고 재조회 또는 사용자 확인 대상으로 남긴다.

---

## 11. 보안 및 패키지

- `.npmrc`의 공급망 보안 설정을 사용자 승인 없이 완화하지 않는다.
- service role, secret API key, JWT, 비밀번호를 코드나 문서에 직접 기록하지 않는다.
- 서버 비밀키를 `NEXT_PUBLIC_` 환경 변수에 넣지 않는다.
- 새로운 패키지는 실제 필요성과 유지보수 상태를 확인한 뒤 추가한다.
- 설치 스크립트 실행이 필요한 패키지는 내용을 확인한 후 사용한다.

---

## 12. 프로젝트 규칙 추가 기준

새로운 경험이나 버그 수정 내용을 `project_rules.md`에 추가하려면 다음 조건을 모두 만족해야 한다.

1. 특정 화면 하나가 아니라 프로젝트 여러 영역에 반복 적용될 가능성이 높다.
2. 구현 방식이 바뀌어도 원칙 자체는 유지된다.
3. 코드만 읽어서는 알기 어려운 중요한 운영 제약이다.
4. `BUSINESS_LOGIC.md`에 들어갈 업무 규칙이 아니다.

위 조건을 만족하지 않는 버그 해결 기록은 `docs/history/SUCCESS_DNA.md` 또는 Git/PR 이력에 남긴다.

---

## 13. 작업 완료 보고 및 검증 원칙

- GitHub에서 직접 확인 가능한 `commit`, `push`, PR, merge, 변경 파일, diff, branch, main HEAD 등은 별도 완료 보고서에 중복 기록하지 않는다.
- 작업 결과는 가능한 한 작업 브랜치의 commit/push 및 PR 이력으로 남겨 GitHub에서 추적 가능하게 한다.
- PR 제목과 commit 메시지는 작업 목적을 식별할 수 있도록 작성하고, 관련 없는 변경을 같은 PR에 섞지 않는다.
- `C:\Users\USER\Desktop\orca\orca-report.md`는 GitHub에서 확인할 수 없는 로컬 검증 결과와 예외사항만 기록한다.
- 로컬 보고 대상은 working tree 상태, untracked/missing 파일, `.env.local` 등 로컬 환경 상태, 로컬 테스트 결과, 브라우저 실화면 검증, 운영 DB 직접 조회 결과, 로컬 자동화/프로세스 오류 등으로 제한한다.
- GitHub에서 확인 가능한 내용을 채팅·PR·보고서에 반복해서 작성하지 않는다.
- 작업 지시문에는 해당 작업에서 GitHub로 확인할 수 없는 로컬 검증이 실제로 필요한 경우에만 별도 보고 항목을 명시한다.
- 사용자가 별도 파일을 공유하지 않아도 GitHub에서 확인 가능한 작업 결과는 GitHub 이력을 기준으로 검증한다.


