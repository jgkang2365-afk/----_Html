# 구현 태스크 목록 - 작업환경측정결과 보고서 처리 통합

이 목록은 TRD를 바탕으로 한 실제 구현 단계입니다.

- [x] PRD, TRD, Task 문서 작성 및 요구사항 확정

## 1단계: 데이터 연동 및 DB 보완
- [x] `lib/sync/excel-sync.ts` 내 이메일 추출 로직 보완 (BL열 파싱 및 정규식 적용)
- [x] `measurement_business` 테이블에 `is_email_sent`, `last_sent_at` 등 상태 관리 필드 추가 (마이그레이션)
- [ ] 엑셀 재동기화 테스트를 통한 이메일 데이터 적재 확인

## 2단계: 메일 발송 및 파일 연동 서비스 구축
- [x] `lib/email/email-service.ts` 생성 (Nodemailer 고정 ID 설정)
- [x] 파일 탐색 유틸리티 구현 (`findReportFiles.ts`)
    - 보고서: 엄격한 규칙 매칭 (`사업장명(보고서-YY상/하).pdf`)
    - 수수료 내역서: 유연한 매칭 (`*수수료 내역서(YY상/하).pdf`)
- [ ] 메일 발송 API (`/api/email/send`) 구현 및 예외 처리 (파일 매칭 실패 시 스킵)

## 3단계: K2B 자동 전송 엔진 이식
- [ ] Playwright/Selenium 라이브러리 설정 (개발 PC 환경 최적화)
- [ ] 기존 파이썬 Selenium 로직의 Node.js 포팅
- [ ] K2B 업로드 자동화 API 구현 및 실시간 진행 상태 연동
- [x] 대시보드 페이지 (`app/(dashboard)/report-processing/page.tsx`) 기본 구조 생성
- [x] 년도/반기 필터 및 업체 목록 테이블 구현
- [x] 보고서 처리 목록 조회 API (`/api/report-processing`) 구현
- [ ] **수신자 이메일 인라인 편집 기능 추가**
- [x] **업체 필터링 및 체크박스 일괄 선택 UI 구현**
- [x] **내비게이션바(Navbar)에 '보고서 처리' 메뉴 추가**
- [ ] 개별/일괄 메일 발송 및 K2B 업로드 UI 연동
- [ ] **K2B 전송 결과의 측정일지 데이터 자동 동기화 로직 구현**

## 4단계: 검증 및 고도화
- [ ] 실제 Z 드라이브 파일 탐색 및 첨부 발송 테스트
- [ ] 인쇄 미리보기 기능 연동 (SummaryTable.tsx 참조)
- [ ] 에러 핸들링 및 사용자 가이드(비밀번호 설정 등) UI 보완

## 5단계: 완료 보고 및 정리
- [ ] 최종 워크스루 작성
- [ ] 레거시 파이썬 코드 정리 가이드 제공
